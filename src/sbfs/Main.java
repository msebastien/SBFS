package sbfs;

// Old Main
public class Main {
    public static void main(String[] args){
        /*File file = new File("foo.txt");
        try {
            Crypto.generateKeyPair("./keys", 2048);
            KeyPair kp = Crypto.loadKeyPair("./keys");
            SecretKey sk = Crypto.generateSecretKey();

            Crypto.encryptFile(file,sk);
            byte[] encryptedKey = Crypto.encryptSecretKey(sk,kp.getPublic());

            File encryptedFile = new File("foo.enc.txt");
            Crypto.addKeyToFile(encryptedFile,encryptedKey);

            Crypto.decryptFile(encryptedFile,kp.getPrivate());
        } catch (Exception e){
            System.err.println(e.getMessage());
            e.printStackTrace();
        }*/

    }
}
